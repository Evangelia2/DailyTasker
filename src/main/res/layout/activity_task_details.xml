<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Task Name -->
    <TextView
        android:id="@+id/taskName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Task Name: Example Task"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Task ID -->
    <TextView
        android:id="@+id/taskId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Task ID: 1"
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- Task Description -->
    <TextView
        android:id="@+id/taskDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Description: This is a sample task description."
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- Task Start Time -->
    <TextView
        android:id="@+id/taskStartTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Time: 10:00 AM"
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- Task Duration -->
    <TextView
        android:id="@+id/taskDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Duration: 2 hours"
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <!-- Task Status -->
    <TextView
        android:id="@+id/taskStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Status: Pending"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Task Location -->
    <TextView
        android:id="@+id/taskLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Location: Office"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Button to mark task as completed -->
    <Button
        android:id="@+id/btnCompleteTask"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mark as Completed"
        android:layout_marginBottom="16dp" />

</LinearLayout>
